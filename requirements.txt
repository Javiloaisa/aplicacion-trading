pandas>=2.3.2
ta>=0.11.0
numpy>=2.0.0
python-dotenv==1.0.1
# ── PWA + Web Push ──
flask>=3.0.0
pywebpush>=2.0.0
waitress>=3.0.0   # servidor WSGI de producción (opcional; si falta se usa el de Flask)
